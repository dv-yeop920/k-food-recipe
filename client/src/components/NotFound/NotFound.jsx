const NotFound = () => {
  return (
    <p
      style={{
        padding: "70px",
        textAlign: "center",
        fontSize: "4rem",
        fontWeight: "600",
        color: "var(--text-color)",
      }}
    >
      검색 결과가 없습니다!
    </p>
  );
};

export default NotFound;
